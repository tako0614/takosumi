{
  "@context": "https://takosumi.com/contexts/v1.jsonld",
  "@id": "https://takosumi.com/kinds/v1/filesystem-object-store",
  "@type": "ComponentKind",
  "name": "filesystem-object-store",
  "version": "v1",
  "description": "Filesystem-backed object-store binding for local or single-host profiles. Publishes a directory-backed bucket endpoint.",
  "family": "object-store",
  "portableBase": "https://takosumi.com/kinds/v1/object-store",
  "referenceAliases": [
    "filesystem-object-store"
  ],
  "publications": {
    "bucket": {
      "contract": "object-store",
      "exampleMaterialMapping": {
        "bucket": "$outputs.bucket",
        "endpoint": "$outputs.endpoint",
        "region": "$outputs.region"
      }
    }
  },
  "capabilityTerms": [
    "local-filesystem"
  ],
  "spec": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "required": [
      "name"
    ],
    "additionalProperties": false,
    "properties": {
      "name": {
        "type": "string",
        "minLength": 1,
        "description": "Logical bucket name (provider applies its own scoping rules)."
      }
    }
  },
  "outputs": [
    {
      "name": "bucket",
      "type": "string",
      "required": true,
      "meaning": "Implementation-scoped bucket name."
    },
    {
      "name": "endpoint",
      "type": "string",
      "required": true,
      "meaning": "S3-class endpoint URL."
    },
    {
      "name": "region",
      "type": "string",
      "required": false,
      "meaning": "Bucket region."
    }
  ]
}
