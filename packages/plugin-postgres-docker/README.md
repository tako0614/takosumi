# @takos/takosumi-plugin-postgres-docker

Docker `KernelPlugin` factory for the Takosumi reference `postgres@v1` descriptor.

```ts
import { dockerPostgresProvider } from "@takos/takosumi-plugin-postgres-docker";
```
