{
  "//": "Workspace-internal source module: folded into the single published @takosjp/takosumi npm package as the subpath @takosjp/takosumi/kind/kv-store (see takosumi/AGENTS.md ‘npm Publish Layout’). The per-folder name/version + jsr: imports here are legacy JSR-era / workspace-member artifacts, NOT an independent publish stream — the npm package version comes from packages/all/deno.json. Do not treat this as a standalone published package.",
  "name": "@takos/takosumi-kind-kv-store",
  "version": "0.1.0",
  "license": "MIT",
  "exports": {
    ".": "./mod.ts"
  },
  "imports": {
    "takosumi-contract/reference/shape": "jsr:@takos/takosumi-contract@^2.6.0/reference/shape",
    "takosumi-contract/reference/shape-validators": "jsr:@takos/takosumi-contract@^2.6.0/reference/shape-validators"
  },
  "tasks": {
    "check": "deno check mod.ts src/**/*.ts"
  }
}
