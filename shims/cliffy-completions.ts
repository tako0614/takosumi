// Bun migration REVIEW stub: @cliffy/command/completions.
// See shims/cliffy-command.ts. Non-functional placeholder so module evaluation
// does not crash; the real CLI needs a commander port (REVIEW).

export class CompletionsCommand {
  // deno-lint-ignore no-explicit-any
  constructor(..._a: any[]) {}
  // deno-lint-ignore no-explicit-any
  action(..._a: any[]): this {
    return this;
  }
}

export default { CompletionsCommand };
